## Emulator launch

```
QT_QPA_PLATFORM=xcb dolphin-emu --exec="emulation/wiigamecube/roms/Pokemon Colosseum (Europe) (En,Fr,De,Es,It).ciso"

python main.py -e "wait_s(22); tap('A'); wait_s(4); tap('A'); wait_s(6); tap('A'); wait_s(2); tap('UP'); tap('A')"
```

## Run back to npc



## Reset attempt

```
python scripts/hunt.py route --phase reset
```
